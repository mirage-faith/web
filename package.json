{
  "name": "mirage-web",
  "private": true,
  "description": "mirage.faith — static site + Worker API",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "check": "node scripts/gen-build-info.mjs && wrangler types --check && tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "wrangler": "^4.127.0"
  }
}
